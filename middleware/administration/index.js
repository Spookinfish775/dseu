export default function ({ store, redirect }) {
    // let claims = store.getters["user/claims"]
    // if (
    //     claims.hasOwnProperty("UserRegistration") ||
    //     claims.hasOwnProperty("UserClaimRoleRegistration") ||
    //     claims.hasOwnProperty("ManipulateWithDataBookEntries")
    // ) {
    // } else {
    //     redirect("/")
    // }
}
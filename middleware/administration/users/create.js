import { PermissionControler } from "~/infrastructure/classes/PermissionControler";

export default function ({ store, redirect }) {
    // let claims = store.getters["user/claims"]
    // if (!claims.hasOwnProperty("UserRegistration"))
    //     redirect("/")

    // if (!PermissionControler.canCreate(claims["UserRegistration"]))
    //     redirect("/")
}